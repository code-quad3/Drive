spring.application.name=uber-backend
server.port=8081
app.cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:5173}
ml.url=${ML_URL:http://ml:8000/predict-eta}
spring.kafka.properties.ssl.truststore.location=${KAFKA_TRUSTSTORE_PATH:/app/secrets/truststore.jks}
